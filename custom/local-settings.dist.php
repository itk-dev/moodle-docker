<?php

// ---- Development settings (/admin/settings.php?section=debugging) ---- //

// Debug = Developer mode.
//$defaults['moodle']['debug'] = 32767;

// Show debug messages in header.
// Set to on, the error reporting will go to the HTML page. This is practical,
// but breaks XHTML, JS, cookies and HTTP headers in general. Set to off, it
// will send the output to your server logs, allowing better debugging.
// The PHP setting error_log controls which log this goes to.
//$defaults['moodle']['debugdisplay'] = true;

// Show parse time in footer.
//$defaults['moodle']['perfdebug'] = '15';

// Show translation strings when ?strings=1 parameter is added to url.
//$defaults['moodle']['debugstringids'] = true;

// If enabled, a partial or full PHP stack trace is added into the SQL as a comment.
// Possible values 0,1,2,100
//$defaults['moodle']['debugsqltrace'] = '100';

// Show validator links in footer.
//$defaults['moodle']['debugvalidators'] = true;

// Show page information in footer.
//$defaults['moodle']['debugpageinfo'] = true;

// If enabled, templates used for rendering are shown as comments in the page HTML
//$defaults['moodle']['debugtemplateinfo'] = true;

// ---- Development settings end. //